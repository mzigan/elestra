{
  "name": "elestra-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "clean": "rm -rf node_modules **/node_modules **/dist"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "pnpm": "^9.1.0",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
